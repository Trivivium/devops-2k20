version: '3.4'

services:
  webapplication:
    image: webapplication
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    restart: on-failure

  db:
    image: "mcr.microsoft.com/mssql/server"
    environment:
       SA_PASSWORD: "ULA2V9sPbG"
       ACCEPT_EULA: "Y"
